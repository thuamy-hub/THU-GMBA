# GMBA 官網 每學期更新 SOP

適用對象：學程助理
最後更新：2026-08-06

---

## 開始前

所有檔案都在這個資料夾：

```
我的雲端硬碟 (amychen@go.thu.edu.tw)＼GMBA 官網管理＼現用版本
```

網站有**中英文各三頁**，共六個網頁檔：

| 英文 | 中文 | 內容 |
|---|---|---|
| index.html | index-zh.html | 首頁 |
| academics.html | academics-zh.html | 課程與師資 |
| admissions.html | admissions-zh.html | 招生與費用 |

**最重要的一條規則：中英文永遠成對更新。** 只改一邊，網站就會前後矛盾。
下面的做法就是為了讓你不必記得這件事。

---

## 一、每學期一定要做的三件事

### 1. 換招生公告（第一格新聞）

改 **`news-pinned.json`**（用記事本打開即可）：

| 欄位 | 意思 |
|---|---|
| `date` | 公告日期，格式 `2026-10-08` |
| `title_zh` / `title_en` | 標題，中英各一 |
| `summary_zh` / `summary_en` | 兩三句說明，寫清楚報名期間 |
| `link` | 在台申請者的報名網址 |
| `link2` | 海外申請者的網址（國際處） |
| `image` | 海報檔名，通常維持 `news-pinned.jpg` |

換海報圖片：把新海報**存成 `news-pinned.jpg` 覆蓋舊檔**。
建議寬度 900px、橫式。不想放圖就把 `"image"` 的值清成 `""`。

> 提醒：招生公告只適用**在台申請者**，海外申請者由國際暨兩岸合作處另行公告。
> 標題和說明請保留這個區別，不要讓海外學生走錯管道。

### 2. 更新費用與申請期程

改 **`site-data.json`**，這一個檔案涵蓋：

- 學費、宿舍費、校外租屋、健保費、生活費、總計
- 學分費、雜費
- 費用表下方的「資料更新日期」← **每次都要改成當天**
- 五條申請管道的申請期間
- 首頁最上方的關鍵數字
- 行事曆 PDF 連結

**看到 `en` / `zh` 兩行就是中英文不同，兩行都要改；只有一行就是中英共用，改一次即可。**

改完存檔，然後執行「套用」（見下方第二節）。

### 3. 檢查連結還活著

點過這幾個連結，確認沒有失效：

- 招生報名系統 `exam2.thu.edu.tw` 各管道
- 宿舍收費標準 `dorm.thu.edu.tw`
- 國際處 `oir.thu.edu.tw`
- 行事曆 PDF

---

## 二、把 site-data.json 套用到網頁

### 做法 A：請 Claude 做（最快）

在 Cowork 對 Claude 說：

> 更新 GMBA 官網學期資料

Claude 會自己讀 `site-data.json`、比對、套用、檢查中英同步，然後回報改了哪幾處。

### 做法 B：自己跑（電腦要有 Python）

在資料夾裡開命令提示字元，先乾跑看看會改什麼：

```
python apply-site-data.py
```

畫面會列出「舊值 → 新值」。確認正確後再真的寫入：

```
python apply-site-data.py --write
```

### 做法 C：完全手動（沒有 Python 也沒有 Claude）

網頁裡每個會變的欄位都帶有 `data-k="欄位名稱"` 標記。
用記事本開啟網頁檔，按 Ctrl+F 搜尋欄位名稱，例如搜 `fee.tuition.nt`，
會找到這樣一行：

```html
<div class="fee-card-nt" data-k="fee.tuition.nt">NT$69,020</div>
```

把 `>` 和 `<` 中間的 `NT$69,020` 換成新金額即可。
**中英文兩個檔案都要改。**

---

## 三、發布

1. 用瀏覽器打開 `index.html`、`admissions.html` 和兩個中文版，眼睛掃一遍
2. 特別確認：費用表數字、申請期間、新聞第一格的招生公告
3. 執行 **`一鍵推送.bat`**
4. 等一兩分鐘，開實際網址確認線上版本已更新

---

## 四、一年一次或不定期

| 項目 | 檔案 | 怎麼找 |
|---|---|---|
| 語言能力門檻 | admissions.html／-zh | 搜尋 `CEFR B1` |
| 申請文件清單 | 同上 | 搜尋 `申請文件` 或 `Documents` |
| 師資異動 | academics.html／-zh | 搜尋 `fv2-card`；每位老師有卡片和 popup 兩處 |
| 班級組成國籍統計 | index.html／-zh | 搜尋 `Southeast Asia` 或 `東南亞` |
| 學術行事曆表格 | academics.html／-zh | 搜尋 `academic-calendar` |
| 學生故事 | student-voices.html | 只有英文版 |

這些改動比較零散，建議交給 Claude 處理，或先問過學程主任。

---

## 五、出事了怎麼辦

**網頁改壞了**
所有檔案都在 git 版本控管裡。跟 Claude 說「把 XXX.html 還原到上一版」即可，
或用 GitHub 網站上的歷史紀錄復原。

**推送失敗**
先確認 Google 雲端硬碟同步完成（工作列圖示不是轉圈圈），再跑一次。

**首頁新聞區只剩一張招生公告卡**
表示 YAHO 即時新聞暫時抓不到，網頁會自己顯示一行說明。
不影響招生公告，過一陣子通常會自己恢復。若持續好幾天，告訴主任。

**在自己電腦上打開 index.html，新聞區只有招生公告**
這是正常的。即時新聞需要透過網站伺服器才抓得到，本機預覽看不到。
以線上網址為準。

---

## 附錄：這些檔案分別是什麼

| 檔案 | 用途 | 助理會動到嗎 |
|---|---|---|
| `news-pinned.json` | 首頁第一格招生公告的文字 | ✅ 每學期 |
| `news-pinned.jpg` | 招生公告的海報圖 | ✅ 每學期 |
| `site-data.json` | 費用、期程、關鍵數字 | ✅ 每學期 |
| `apply-site-data.py` | 把上面的資料套進網頁 | ⚙️ 執行，不要改內容 |
| `extract-site-data.py` | 反向：從網頁讀回資料 | ⚙️ 通常用不到 |
| `robots.txt` / `sitemap.xml` | 給搜尋引擎與 AI 爬蟲看的 | ❌ 不用動 |
| `functions/api/yaho-news.js` | 抓即時新聞的中介程式 | ❌ 不用動 |
| `一鍵推送.bat` | 發布到網站 | ⚙️ 執行 |
| `六個 .html` | 網頁本體 | ⚠️ 盡量透過上面的 json 改 |
